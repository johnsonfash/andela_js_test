/* eslint-disable linebreak-style */

// Helper functions to automate some processes can go here
const truncateStringToLength = (string, maxLength) => `${string.slice(0, maxLength)} ...`;

export default {
  truncateStringToLength
};
